{
    "name": "email-finder-api-proxy",
    "version": "1.0.0",
    "description": "Cloudflare Workers proxy for Email Finder API with caching",
    "main": "src/index.js",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}