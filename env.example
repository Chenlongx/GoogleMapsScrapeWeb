# 环境变量配置示例
# 复制此文件为 .env 并填入真实的配置值
#
# ⚠️ 重要安全提醒：
# 1. 永远不要将 .env 文件提交到 Git
# 2. ADMIN_SECRET_KEY 必须是强密码（至少32位）
# 3. 定期轮换所有密钥
# 4. 在生产环境中必须设置所有环境变量

# Supabase 配置
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here
# Email Finder 专用（需要 Service Role Key 用于自定义表操作）
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Resend API 配置
RESEND_API_KEY=your_resend_api_key_here

# 支付宝配置
ALIPAY_APP_ID=your_alipay_app_id_here
ALIPAY_GATEWAY=https://openapi.alipay.com/gateway.do
ALIPAY_PRIVATE_KEY=your_alipay_private_key_here
ALIPAY_PUBLIC_KEY=your_alipay_public_key_here

# 环境设置
NODE_ENV=development

# GitHub API Token (用于获取仓库发布信息)
# 重要性: 强烈推荐 ⭐⭐⭐⭐⭐
# 作用: 提高 GitHub API 请求限制从 60次/小时 提升到 5000次/小时
# 如何获取:
#   1. 访问 https://github.com/settings/tokens
#   2. 点击 "Generate new token" → "Generate new token (classic)"
#   3. 选择权限: public_repo (用于访问公开仓库)
#   4. 生成并复制 token (格式: ghp_xxxxxxxxxxxx)
# 注意: 此 token 只需要 public_repo 权限即可，无需其他权限
GITHUB_TOKEN=your_github_token_here

# 管理员密钥 (用于安全面板访问)
# ⚠️ 必须设置！否则任何人都可以访问管理员功能！
# 建议使用至少32位的随机字符串
ADMIN_SECRET_KEY=your_secure_admin_key_here

# ===========================
# Email Finder 认证系统配置（自定义表版本）
# ===========================
# JWT 密钥（用于生成访问令牌，请使用强随机字符串）
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars

# 密码重置后的重定向 URL（可选，默认为 Chrome 扩展地址）
PASSWORD_RESET_REDIRECT_URL=chrome-extension://your-extension-id/reset-password.html
